{
  "name": "auto-one-monorepo",
  "private": true,
  "scripts": {
    "start:server": "cd server && npm install && npm start",
    "start:client": "cd client && npm install --legacy-peer-deps && npm start",
       "dev": "concurrently \"npm run start:server\" \"npm run start:client\"",
       "deploy:client": "cd client && npm run deploy"
     }
    ,
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
